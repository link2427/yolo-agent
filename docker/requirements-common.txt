# yolo-agent Python 3.11 environment — shared package set.
#
# Installed into /opt/pyenv and inherited by EVERY yolo-agent image (base, cpp,
# and reverse-engineering), where it is the only Python environment a user
# needs to think about. The reverse image layers
# docker/requirements-reverse.txt on top of this SAME venv rather than creating
# a second one.
#
# Every pin below was verified to publish a CPython 3.11 / linux-amd64 wheel
# (or a universal py3 wheel), so `pip install -r` needs no compilation.
# Packages whose current release requires Python >= 3.12 are pinned to their
# last 3.11-compatible release on purpose — see PINS.md.
#
# To add a package: add the exact pin, then confirm a usable wheel exists:
#   pip download --only-binary=:all: --python-version 3.11 -d /tmp/x <pkg>==<ver>

# --- HTTP / APIs -------------------------------------------------------------
requests==2.34.2
httpx==0.28.1
urllib3==2.7.0
certifi==2026.7.22
websockets==17.1

# --- CLI / text / terminal ---------------------------------------------------
click==8.5.0
typer==0.27.2
rich==15.0.0
pygments==2.21.0
colorama==0.4.6
tabulate==0.10.0
tqdm==4.70.1
questionary==2.1.1
prompt-toolkit==3.0.53
wcwidth==0.8.3

# --- data / config -----------------------------------------------------------
pydantic==2.13.5
pyyaml==6.0.3
tomli==2.4.1
tomli-w==1.2.0
json5==0.15.0
python-dotenv==1.2.3
packaging==26.3
python-dateutil==2.9.0.post0
arrow==1.4.0
jsonschema==4.26.0

# --- numerics -----------------------------------------------------------------
# numpy >= 2.5 requires Python >= 3.12; 2.4.6 is the last cp311 release.
numpy==2.4.6

# --- HTML / parsing ----------------------------------------------------------
lxml==6.1.3
beautifulsoup4==4.15.0
html5lib==1.1
markdown-it-py==4.2.0

# --- files / system ----------------------------------------------------------
gitpython==3.1.62
pathspec==1.1.1
filelock==3.32.6
platformdirs==4.11.8
psutil==7.2.2
watchdog==6.0.0
tenacity==9.1.4
structlog==26.1.0

# --- testing -----------------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.168.0

# --- lint / format -----------------------------------------------------------
ruff==0.16.7
black==26.5.1
isort==9.0.1
flake8==7.3.0

# --- interactive shell -------------------------------------------------------
ipython==9.17.1
